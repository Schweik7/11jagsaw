# gemini3发布后，仅一次即解决，耗时1分钟
你是一位精通算法竞赛（Competitive Programming）和底层优化的Python专家。请帮我解决一个极高难度的“多联骨牌（Polyomino）精确覆盖”变体问题。

问题描述：
在 10x10 的网格（100个格子）中，放入 11 个 J 形块。
J 形块的形状定义为（坐标）：`[(0,0), (0,1), (1,0), (2,0), (2,1), (2,2), (2,3), (2,4)]`（共8格）。
* 注意： 11块 x 8格 = 88格被占用，剩余 12 格必须留空。
* 变换： 拼图可以旋转（90/180/270度）和镜像（翻转），共8种变体。
# 如果某个AI没能成功，再复制下面的方法给它
核心难点：
普通的搜索算法（如基础回溯或标准的Dancing Links DLX）无法解决此问题，因为解空间巨大且“留空位置”是不确定的。必须使用位运算（Bitmask）配合强力剪枝。

请严格按照以下架构要求编写 Python 代码：

1.  状态表示（State Representation）：
    * 必须使用一个 100-bit 的整数（Integer）来表示棋盘状态，严禁使用二维数组或坐标列表进行递归过程中的状态维护。
    * 碰撞检测使用 `&`，放置使用 `|`，移除使用 `^`。

2.  预计算（Precomputation - Space for Time）：
    * 程序启动时，生成 J 形块的所有 8 种变体，并计算它们在 10x10 网格上所有合法的平移位置。
    * 将这些位置转换为位掩码（Integer Mask）。
    * 关键索引结构： 创建一个列表 `moves_at_index[100]`。对于每一个预计算的掩码，找到它占用的最小索引位（Lowest Set Bit） `i`，将该掩码存入 `moves_at_index[i]`。这用于后续的确定性搜索。

3.  搜索策略（Search Strategy - Lowest Bit Determinism）：
    * 使用 DFS 回溯。
    * 在每一步，不遍历整个棋盘寻找位置。
    * 必须使用 `lowbit` 方法（如 `~board & -~board` 或 `bit_length`）找到当前棋盘上索引最小的空位（First Empty Cell）。
    * 面对这个空位，仅允许两个分支：
        * 分支 A（填入）： 尝试从 `moves_at_index[first_empty]` 中取出一个掩码放入（仅当无碰撞时）。
        * 分支 B（废弃/空洞）： 如果当前已用的“空洞额度”小于 12，可以将该位标记为“占用/障碍”（即跳过该格，视为废弃），递归继续。

4.  强力剪枝（Pruning - Connectivity Check）：
    * 这是解出11块的关键。 在每次递归前，检查剩余空白区域的连通性。
    * 必须实现一个纯位运算的 Flood Fill（泛洪填充）算法，不要使用 BFS/DFS 队列或坐标转换。
    * 通过位移操作（`<<1`, `>>1`, `<<10`, `>>10`）并在循环中与边界掩码（防止左右穿墙）进行 `&` 操作来扩散。
    * 计算所有孤立空区域的 `popcount`（1的个数）。如果任何一个区域的大小无法被 8 整除（或者更精确地说，所有区域能容纳的块数总和 < 剩余目标块数），则立即回溯。

5.  可视化：
    * 找到解后，使用 `matplotlib` 绘制 10x10 网格，不同的块用不同颜色区分，空洞留白。

代码要求：
* 代码需要高效，包含必要的注释解释位运算逻辑。
* 将目标数量设为 11。

请直接给出完整的、可运行的 Python 代码。
